import { View } from "react-native";

import { ChatEditorStyles } from "./style";

export default function ChatEditorScreen( ) {
  return (
    <View style={ChatEditorStyles}>
      
    </View>
  );
};